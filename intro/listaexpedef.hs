calculo x = 4/3 * pi * sin (x ^ 2) - 1

divisao x y = (x^2) * (y^3) / (x-y)^2

contaComplexa x y = 1/(x^2 - y) - (exp 1)^(-4 * x) + (35/y) ** (1/3) * (x * y) ** (1/2) 

contaMaisComplexa x y = 24 + 4 * 5^3/(exp 1) ** 4*4 - logBase 10 12560

contaMaisComplexaAinda x y = (cos (5 * pi)/6) * ((sin (7 * pi)/8) ** 2) + tan (pi/6  * log 8)/7 ** 1/2 + 2

dobro x = x + x

quadruplo x = dobro (dobro x)

areaCirculo r = pi * (r ** 2)

areaTriangulo a b c = sqrt (s * (s - a) * (s - b) * (s - c))
  where
    s = (a + b + c) / 2

minhaFuncao x = 3 + f x + f a + f b  
  where
    f x = x + 7 * c 
    a = 3 * c 
    b = f 2
    c = 10


n = a `div` length xs
  where
    a = 10
    xs = [1,2,3,4,5]

quadradoDoDobro x = (x + x) ** 2

dobroDoQuadrado x = (x ** 2) + (x ** 2)

ladosTriangulo a b c = a > (b + c)|| b > (a + c) || c > (a + b) 

energia k x =  k * x

compressao k x = 1/2 * k * x**2

custoEnergia salario consumo = (salario * 1/5 * consumo) - ((salario * 1/5 * consumo) * 15/100) 

freqRes l c = 1 / (2 * pi * ((l * c) * 1/2)) 

areaTri a b c = (c * h)/2
  where
    cos a = (b ** 2) + (c ** 2) - (a ** 2)/(2 * b * c)
    sin a = (1 - ((cos a)** 2)) *  1/2
    h = b * sin a 
